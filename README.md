### Сразу начну с замечания

У меня не получилось поднять airflow. Я пытался это делать просто отдельно из папочки, пытался собрать в докере, но всё время какие-то ошибки,
которые я уже перестал понимать.

Отдельно в папке project/data_preprocessing.py лежит фунция для дообучения модели и я её запускал из терминала. В Airflow ни в какую не поднимается


В файле app.py собрана основная логика работы сервисов.
Их несколько:
Стандартный с Recommendations как в теории курса - отдает готовые реки, или популярные.
Сервис SimilarItems - для похожих айтемов
EventsStore - сохраняет последние действия пользователя. Применяется в сервисе SessionRecommendations.


Для поднятия сервиса в докер:

cd mle-pr-final
source .final_venv/bin/activate

docker compose up --build

Для проверки работоспособности 
В соседнем терминале:
python test.py


Эндпоинты
http://localhost:8000 - Основной сервис
http://localhost:9090 - Prometheus
http://localhost:3000 - Grafana


Наброски дашборда прикрепил скрином, так как в прошлый раз из-за проблемы с датами у ревьюера не отображалось.
Я над ним не поработал из-за недостатка времени, но я умею)))